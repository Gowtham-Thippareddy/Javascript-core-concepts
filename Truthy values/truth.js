console.log(Boolean("")) // false, because there is no data
console.log(Boolean(0)); // 0 means false 
console.log(Boolean(null)); // null means false
console.log(Boolean(undefined)); // false
// empty array
console.log(Boolean([])); // ture , we can assigin the values in the array
console.log(Boolean('A')); // true , because value is assined.
// empty object
console.log(Boolean({})); // object is empty , here we can assigin a value
//empty
console.log(Boolean(function demo(){}))
